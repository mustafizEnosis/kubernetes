- name: Loop through dictionary values
  hosts: localhost
  tasks:
    - name: Print dictionary values
      debug:
        msg: "{{ item.value }}"
      with_dict: 
        my_dict:
          key1: value1
          key2: value2
          key3: value3
